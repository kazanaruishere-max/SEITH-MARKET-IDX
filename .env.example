# .env.example — COPY to .env, never commit .env
# SECURITY: SECTORS_API_KEY = server-only, never to client/log. Rotate via portal if leaked. Former key 0843... revoked 2026-09-05.
SECTORS_API_KEY=
SEITH_API_KEY=
SEITH_LLM_MODEL=SEITH-MARKET-IDX
SEITH_LLM_FALLBACK_MODEL=Seith-AI-Trading
MARKET=id
LLM_BASE_URL=http://localhost:20128/v1
KRONOS_URL=http://localhost:8001
ANALYSIS_URL=http://localhost:8002
RUST_LOG=info
