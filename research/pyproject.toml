[project]
name = "seith-research"
version = "0.1.0"
description = "research isolated - plotly offline only, no apps/kronos-sidecar pollution"
requires-python = ">=3.11"
dependencies = ["plotly==5.24.1", "nbformat", "nbconvert", "ipykernel"]

[tool.ruff]
line-length = 100
