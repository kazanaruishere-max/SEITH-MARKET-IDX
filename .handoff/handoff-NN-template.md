# Handoff NN — Topic

## Goal
One-sentence win objective.

## Context
Reference AGENTS.md + docs/* + skill seith-market-intelligence. Market=id default, CompositeCache.

## Scope In / Out
In: ...
Out: ...

## Deliverables
- File list + acceptance

## Todo (`todowrite` WAJIB — AGENTS §8d)
- [ ] Buka todo `in_progress` sebelum Implement; `completed` hanya setelah Verification hijau.

## Verification
`cargo fmt --check && cargo clippy -- -D warnings && cargo test` (+ uv/ pnpm if touched)

## Next Session Prompt
`skill://seith-market-intelligence` + branch `handoff/NN-topic` + task slice
