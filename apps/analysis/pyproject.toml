[project]
name = "seith-analysis"
version = "0.1.0"
description = "TradingAgents-Lite 3-agent (Fund/Tech/Synth) → 9router :8002"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.29",
  "pydantic>=2",
  "httpx>=0.27",
]

[tool.ruff]
line-length = 100

[dependency-groups]
dev = ["pytest>=8", "httpx>=0.27", "ruff>=0.9"]
