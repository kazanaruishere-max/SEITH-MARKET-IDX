# ADR 0002 — Wire SCHEMA_VERSION + Vendor Pin + Envelope

- Envelope `{success,data,error,pagination}` + `SCHEMA_VERSION 1.0.0` header. Evolusi via version bump, bukan loosen validator.
- Vendor `vendor/Kronos` (`67b630e67f6a18c9e9be918d9b4337c960db1e9a` 2026-09-05, MIT) dan `vendor/TradingAgents` (`9dee508c44662702281a8dbaad1f7b42179b5ba7` 2026-09-05, Apache-2.0) — shallow `git submodule --depth 1` read-only reference; workflow copied minimal to sidecars (`apps/kronos-sidecar` :8001, `apps/analysis` :8002 → 9router). No `git pull` without ADR + `scripts/update-vendor.ps1` + hash bump here.
- Weights Kronos-base 102.3M via HF `NeoQuasar/Kronos-base` + `Tokenizer-base` (HF cache, not in vendor).
- Secrets never to log/client (see `crates/seith-core/src/redact.rs`). Repo public 19 Aug–30 Sep 2026, freeze on submit. Clone: `git clone --recurse-submodules --depth 1 <url>`.

Update procedure: `.\scripts\update-vendor.ps1 -Vendor Kronos -Ref master` → record new hash here + `vendor/README.md`.
